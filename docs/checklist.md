# Project checklist

This is a living checklist for the Tourism Geography Tutor / Course Brain project. It's grounded in the original blueprint (`fyp cb.pdf`, in the Google Drive folder `Aztech Digital/Tourism Chatbot`) and is meant to be appended to, not rewritten, as new client feedback comes in.

**How to use this doc:**
- Check items off (`- [x]`) as they ship.
- When new client feedback arrives, add a new dated subsection under "Client feedback log" rather than editing prior entries — keep the history intact.

## Blueprint feature checklist

From the original mockup (`fyp cb.pdf`, Figures 3.1–3.8), against the actual state of the codebase:

- [ ] **Homepage/landing with chatbot framing** — partial: `web/src/app/page.tsx` exists but is a plain chapter/topic index, not the blueprint's marketing hero with mascot + "Start Chat"
  - **Update (2026-08-16):** the landing experience now has clear **Start learning** and **Ask the tutor** actions plus a short Read → Ask → Practise orientation flow and consistent global navigation. The item remains open only because the blueprint-specific mascot treatment has not been implemented or approved.
- [x] **Conversational chatbot Q&A interface** — live (2026-08-08): `TutorPanel` is rendered on the chapter page. `grounding.ts`'s keyword-overlap matcher runs first (free); a DeepSeek fallback (`web/src/lib/tutor/llm-fallback.ts`, `deepseek-v4-flash`) now handles questions it misses — typos, loose phrasing, or questions about topics other than the one currently open. Every answer's citation is a real link back to its source content block, which briefly highlights on arrival.
- [ ] **Topic selection by category** (Tourism Destinations, Climate, Physical Geography, Human Geography, Transportation & Access, Cultural Heritage) — model mismatch: current app organizes content by Chapter → Topic drawn from the actual DTM10333 course PDFs, not this 6-category taxonomy from the mockup. Open decision: keep the course-chapter structure, or reorganize navigation around these 6 categories.
- [ ] **Rich answer format** (definition + bullet points + "Example" + "Importance" + related-SDG badges + "Suggested Reading") — partial (2026-08-08): `ContentUnit` now renders a `content_type` badge and visually separates `example` blocks; all 66 published units were reclassified from a single generic type into 44 definition / 7 example / 6 explanation / 6 key_takeaway / 1 case_study / 2 learning_note. Still missing: "Importance" section, SDG badges, "Suggested Reading".
- [ ] **Learning Materials page** (chapter list with View/Download) — partial: chapters/topics are listed on the home page, but there's no dedicated materials page and no view/download links to source PDFs
- [x] **Quiz/Self-Assessment** (multi-question flow with progress bar + final score) — partial: `QuizCard` + `/api/quiz/answer` work, but `quiz_questions` has **zero rows** in the DB (verified 2026-08-08) — nothing renders today regardless of the single-question-cap limitation. No multi-question sequence, no score summary.

  - **Update (2026-08-12):** assessment delivery is built: topic quiz (3 MCQ/2 written), chapter mini exam (5/3), and full four-chapter exam (10/6) generate randomized approved-only batches, show a score and answer schemes after submission, and provide **Which to refer** links to the cited source unit. Written answers are graded by the installed Tutor/DeepSeek flow with narrow synonym/typo handling. Draft banks now exist for CH2–CH4 and one CH1 topic, but remain deliberately invisible until a future lecturer approval/login decision; this item is not complete until approved question records exist.

  - **Update (2026-08-12, final):** all four reviewed generated banks are now imported: CH1 53, CH2 75, CH3 30, CH4 50 (208 total). The owner deferred login/lecturer approval and enabled these source-validated `draft` / `deepseek_draft` questions for practice; answer keys/rubrics remain server-only until submission. The final production deployment passed Vercel’s build and TypeScript check.
- [x] **About/User Guide page** — not built: no `/about` route exists
  - **Update (2026-08-16):** `/about` now explains the Read → Ask → Practise flow, the citation/trust model, assessment choices, and direct entry points into Chapter 1 and the full exam.
- [x] **Backing content actually published** — resolved: this item was stale. Verified 2026-08-08 by querying the live DB directly that all 66 content units were already `status: published` — the app was serving real content the whole time, just undifferentiated by type (see "Rich answer format" above).
  - **Update (2026-08-08, later same day):** the original text-layer extraction (`scripts/extract_course_materials.py`) skips image-only/scanned pages (`needs_ocr`), which turned out to be a real, significant gap, not a minor one — e.g. Chapter 2 lists "high latitude" and "highland" as 2 of its 5 major climate types but had **zero** content for either, because those sections were image-heavy slides the text extractor couldn't read. Did a full page-by-page visual read (not OCR — direct vision reading) of all 4 chapter PDFs to find what got missed. Result: DB grew from 66 → 86 published content units, 19 → 21 topics (added "High latitude climate" and "Highland climate" under Chapter 2), and Chapter 2's chapter title was corrected from the placeholder `"CH2"` to its real title, **"World Climate"** (read directly off the deck's title slide). Full scan notes, including what's already covered vs. still-unchecked pages, saved at `data/extracted/vision-scan-2026-08-08.md` so this doesn't need re-scanning from scratch later.
  - **Content-accuracy flag for the client/lecturer:** `chapter-3.pdf` page 2 states there are "nine planet[s]" and names an ocean "Hindi" (likely meant "Indian") and "Artic" (Arctic) — these look like errors/typos in the original slide. Not imported into the DB as fact; flagging rather than silently correcting, since fixing course content is a call for whoever owns the material.
  - **Update (2026-08-09):** enriched Chapter 3's "Hydrosphere"/"Lithosphere" units (`171860d9-25fc-485b-bac2-20cc69fbb0a3` topic) with the 5-ocean/7-continent lists page 2 actually names — used the corrected spellings ("Indian", "Arctic") for the two typo'd ocean names above, matching the spelling already used for Chapter 2's ocean units, rather than reproducing the slide's typos verbatim. Also same page 2: the Equator-diameter fact ("7,926.4 mile[s] from east to west and **7,899.8 mile[s] from north to east**") literally says "north to east," which doesn't describe a coherent diameter — almost certainly meant "north to south" (polar diameter, paired with the equatorial east-west figure). New content unit "The Equator" (id `59312e55-7f96-4ac8-8d00-65ad610a5955`) renders it as "north to south"; flagging the literal source wording here rather than silently carrying the apparent typo into a user-facing fact.

## Client feedback log

### 2026-08-08

- [ ] **Examples grounded in slides** — author `example`-type content units (data/content work during the review step) so each topic's notes surface a slide-sourced example, not just definitions; no new schema needed (the `example` content_type already exists), just content authoring + a UI branch in `ContentUnit` to visually separate example blocks
  - **Status (2026-08-08):** partial. The UI branch shipped, and reclassifying the existing 66 units by type recovered `example` content for 4/19 topics (where the text already named concrete things, e.g. Mount Everest, the Sahara, named plateaus). The other 15 topics still need genuinely new example content authored from the real source slide PDFs — those aren't in this repo checkout (`data/course-materials/` is gitignored and empty), so this is blocked on the client/content-owner supplying them.
  - **Update (2026-08-08, later same day):** got the source PDFs and did a full visual scan (see the "Backing content actually published" update above) — added one genuine new example ("The Prime Meridian as a tourist attraction," Chapter 3) plus 19 other new definition/explanation/key_takeaway units recovered from previously-unreadable slides. Topic coverage for `example`-type content is still incomplete; most topics still have none.
- [x] **"Let's make a quiz" CTA per topic/chapter** — add a link/button at the end of a topic's content units pointing to that topic's quiz section; can reuse the existing `?topic=<uuid>` query-param routing with an anchor (e.g. `#quiz`) for a same-page jump, or a dedicated `/quiz` route if quiz-only real estate is wanted
  - **Status (2026-08-08):** not started.
- [x] **Chatbot section for questions** — re-enable `TutorPanel` on the chapter page (component + API route already exist, just unrendered) as a first step; note the blueprint's Figures 3.2/3.4 imply a persistent chat thread, while today's `TutorPanel` is a single-shot Q&A box with no message history — decide whether re-enabling as-is is enough for now or whether a real chat UI is in scope for this round
  - **Status (2026-08-08):** done, and taken further than "re-enable as-is" — see the blueprint checklist's "Conversational chatbot Q&A interface" item above (global search + DeepSeek fallback + clickable/highlighting citations). Still single-shot, no message history — that part of the open decision above remains unaddressed.
- [ ] **Interactive images for definitions** ("define an ocean" → image) — today, diagram images are mapped only per-topic (`diagrams.ts`, keyed by topic UUID, one diagram per topic); showing an image per-definition needs either extending that map's key scheme to content-unit IDs, or a new column/table — this is a data-model extension, not just a UI change
  - **Status (2026-08-08):** not started.
- [x] **Flashcard section** — net-new feature, no existing flashcard code anywhere in the repo; the `content_type` filter (e.g. `definition`, `key_takeaway`) gives a ready-made dataset to drive flashcards from once content is published; no flip/swipe animation library is installed yet (no framer-motion or similar)
  - **Status (2026-08-08):** not started. The `content_type` dataset this needs now actually exists (see reclassification above), so this is more buildable than when originally written.
  - **Update (2026-08-16):** complete. The source-grounded `/flashcards` experience uses 103 published definition/key-takeaway units and includes chapter/topic filters, shuffle, reveal, self-rating, a review-again deck, citations, and direct learning-note links. It uses the existing interface tokens and CSS transitions, so no animation dependency was needed.
- [x] **Colorful UI** — currently pure Tailwind v4 defaults (`slate-*` grays only, no `@theme` tokens, no design-tokens file); a color pass would start in `web/src/app/globals.css`'s `:root` block, which has no custom tokens defined yet
  - **Status (2026-08-08):** explicitly deferred to backlog per client decision this session — not scoped for the current round.

### 2026-08-09

- [x] **Content oversimplified — individual entities merged into single units** — client caught that Chapter 2's 7 continents and 5 major oceans didn't have individual descriptions (compressed into 3 "Compare X and Y" style units instead of 7 + 5 = 12). Audited and fixed across **all 4 chapters**, not just the reported case: found and split 9 total instances of this pattern (continents/oceans, summer/winter monsoons, sea/ocean, river/lake, domestic/international/inbound/outbound tourism, travel arrangement types, long-haul/short-haul). DB grew from 86 → 111 published content units in this pass. Standards written up in a new `.claude/skills/course-content/SKILL.md` skill so this doesn't recur — the core rule is one distinct named entity gets its own content_unit, even if several share a source page. One genuine content gap (not a merge issue) was flagged, not fixed: chapter-2.pdf p19 names 3 forest types under Humid Subtropical climate with no content_unit yet.
- [x] **Chapter 1 content-quality fixes (Opus review pass)** — chapter title "Chapter 1" → "Tourism Geography" (p1 title slide). Reordered "Geography foundations" topic so both geography definitions are adjacent. Added a missing unit transcribing the Push-Pull Model diagram's own factor lists (previously the diagram had zero supporting text, same class of gap as Chapter 4's mountain-ranges diagram) and added the Williams & Zelinsky (1970) attribution p21 gives for the distance/connectivity unit. Added a missing framing unit for "Measuring tourist flows" (p22's "three types" framing sentence) and aligned three sibling units to the same `content_type` (`definition`) plus enriched two bodies with p23 detail. **Split "Forms and markets of tourism"** (14 units, effectively 3 sub-topics glommed together per its own p24 contents slide) into three new topics — "Forms of tourism," "Tourism markets and products," "Types of travel arrangement" — each reordered to source-page order; added a `case_study` unit for the Tourism Queensland (2002) seniors-market review p31 names, and split a merged youth/gay/grey tourism unit into individual "Youth tourism" and "Gay tourism" units (grey already had its own). Added a closing synthesis unit for "Travel distance" from p33. Fixed one pipeline-voice body.
  - **Found but not acted on (future enrichment, not this pass's scope):** p28/p29 have richer detail than their current bodies capture (common-interest/business traveller specifics); p4/p5 geography-definition bodies drop minor clauses (planets/moons/asteroids; international/multicultural/environmental education).
- [x] **Chapter 2 content-quality fixes (Opus review pass)** — chapter title "World Climate" → "World Geography and Climate" (the chapter covers continents/oceans too, not just climate). Renamed "Continents and basic world geography" → "The seven continents" (the old name was a vestige of the now-fixed ocean-miscategorization bug) and reordered its list unit's body to match the individual continent units' sequence; same fix for "Major oceans," plus moved its synthesis unit ("Continents and their surrounding oceans") to render last instead of third. Added a missing "Climate zones by latitude" opener for "World climate classification" (p13), a missing general lead-in for "High latitude climate" (p21), and a missing "Highland climate examples" unit naming Scandinavia/Norway/Sweden/Iceland (p23). Reordered "Tropical climate" so the two monsoon units sit next to the tropical-humid unit they detail. Rewrote 8 remaining slide-objective-style titles ("Identify the seven continents" → "The seven continents," etc.) to clean noun phrases. Fixed pipeline-voice phrasing in 3 more Chapter 2 units.
- [x] **Relevant photos on content** — client wants real photos, extracted from the actual source slides (not stock/generic), attached to content. Confirmed technically feasible: real embedded images can be pulled directly out of a PDF's internal structure via `pdf-lib` with no rendering tools needed (this machine has neither `poppler`/`pdftoppm` nor `pip` available). Schema decision made and shipped: `web/src/lib/course-brain/content-images.ts` (`Record<contentUnitId, ContentImage>`, files in `web/public/content-images/`, no Supabase Storage needed) — `ContentUnit` already renders a `<UnitImage>` wherever an entry exists. Still an **empty scaffold**, zero entries populated.
  - **Update (2026-08-10):** client also flagged the content itself as too shallow, not just missing photos ("do for the others as well, not just the part i flagged"). Ran a full page-by-page content-depth-and-photo audit against all 4 source PDFs, saved at `docs/content-depth-photo-audit-2026-08-09.md` — every chapter done, ~40-45 real photo/diagram opportunities identified with page/position/confidence, plus a running list of shallow content_unit bodies, one citation error, and a few genuinely-missing tables. Build/fix pass (extracting the photos, enriching the bodies) not started yet — see `docs/handoff.md` for the next step.
- [x] **Chapter 4 content-quality fixes (Opus review pass)** — verified each finding against `chapter-4.pdf` directly (per-page vision reads, not batched, after a batched multi-page read silently dropped some pages) before writing anything. Fixed: merged two page-4 near-duplicate units into one ("Tourism resources are often shared with other users," second sentence now covers coordination); retyped the mountain-as-barrier unit from `case_study` to `example` (it's one sentence, not a narrative scenario); split "Plateau examples named in the extraction" (pipeline-voice title, and page 17 genuinely gives each plateau its own detail) into 3 individual units — Tibetan/Antarctic/Andean Plateau; added grouped units for the p15 highest-mountains table and the p16 mountain-ranges map (the latter now has diagram support in `diagrams.ts`); added a grouped p19 "Highest island peaks" unit; moved "Island" (p19, "Island and Beaches") from "Natural landscapes and landforms" into "Water environments," and reordered that topic so basic categories (Ocean/Sea/River/Lake/Island) precede the fine-grained glossary terms instead of following them; extracted the p23 labelled floor-plan diagram (`ch4-hydrosphere-lithosphere-floor-plan.png`) via `pdf-lib` raw-stream extraction and wired it into `diagrams.ts` for "Water environments" (previously had no diagram). Checked and found already-clean: p8's attraction-category bare list, and unit `15c93c2f`'s body (no pipeline-voice phrasing present).
  - **Flagged, not acted on:** the p23 floor-plan diagram labels item 13 "River" a second time (item 10 is also "River") next to a lake-shaped blue oval — looks like a mislabelling in the source (probably meant "Lake"); kept the extracted image as-is rather than relabelling. Also found two more tables not yet captured — p19's "Continental landmasses" highest-point table (Everest/Aconcagua/Vinson Massif/Kosciuszko) and p20's sea/ocean ranking-by-area table — genuinely new content but outside this pass's explicit scope, left for a future pass.

### 2026-08-11

- [x] **Started the DB-write half of the content-depth/photo fix pass** (photo extraction was already ~40% done via `content-images.ts`; this is the Supabase side, from `docs/content-depth-photo-audit-2026-08-09.md`'s findings). This session:
  - Fixed the known citation error: Chapter 2 unit `e6f73628` "Tundra climate" was cited to `chapter-2.pdf` p21, corrected to p22 (its content is actually on p22).
  - Added the 3 missing-table content units the audit flagged as having no unit at all: Chapter 4 "Largest deserts" (p18, 9 named deserts by area) and "Continental landmasses" (p19, highest point per continent) under "Natural landscapes and landforms"/"Water environments," and "Largest bodies of water by area" (p20, 10 named oceans/seas) under "Water environments." All published with `source_references` rows, verified live on the deployed site.
  - DB grew from 125 → 128 published content units.
  - **Still open from the audit** (see "Recommended extraction order" section of the audit doc and the standing next-step list in `docs/handoff.md`): the larger body-enrichment pass — several dozen `content_units.body` rows across all 4 chapters flagged **SHALLOW** (missing named examples, numbers, or sub-details their source page actually gives) — has not been started. Structural gaps (Chapter 4's "valley" and "beach" headings with no source content) remain flagged-only, per the source-fidelity rule — not fixed, not invented.
- [x] **Completed the body-enrichment pass** — every **SHALLOW** finding in `docs/content-depth-photo-audit-2026-08-09.md` fixed, all 4 chapters. 41 `content_units.body` rows rewritten to restore dropped named examples, figures, and sub-facts (source-faithful — nothing invented; a few previously-invented clauses that weren't on the actual slide were trimmed instead, e.g. Chapter 2's Subarctic-climate "boreal forest," Australia's "sometimes called an island continent," Marine-west-coast's invented causal clause, and North America's misattributed Isthmus-of-Panama sentence which actually belongs to South America). Also split the client's originally-flagged case — Chapter 2's Humid Subtropical Climate's three named forest types — into their own units (**Deciduous forest**, **Evergreen forest**, **Mixed forest**), per the project's one-entity-one-unit rule. DB grew from 128 → 131 published content units. Spot-checked multiple chapters live (including the two worst offenders, Chapter 1's "Tourist destinations" and "Business and professional tourism") — all render correctly.
  - **Flagged, not fixed, per the source-fidelity rule:** two more structural gaps (Chapter 4's "beach" heading on p19, no beach content anywhere, same class as p18's already-known "valley" gap) and two more source defects — Chapter 4 p21's heading reads "G. SEA AND OCEANS" but the slide is entirely about rivers and lakes (p20 is the real sea/oceans page); Chapter 1 p11's text begins mid-sentence ("…the human activities that involved the tourism can be spatial…"), missing its own opening words, and p10 (a pure section divider) doesn't carry the missing half.
  - **Not done this pass, left for later:** a handful of very-low-value missing items the audit explicitly deprioritized — Chapter 4 p21's alphabetical fragment of North American river names, and Chapter 4 p17's three plateau photos (ambiguous which plateau each depicts). Chapter 4's Mount Kinabalu case-study unit (p13/p14, currently photo-only, no content unit at all) is content creation tied to the photo pass, not a shallow-body fix — deferred to the photo-extraction track.
- [x] **Completed Chapter 2's photo-extraction track — the whole chapter is now done.** Wrote a reusable `pdf-lib`-based extraction script (walks embedded image XObjects, handles both `DCTDecode` JPEGs directly and `FlateDecode`/`DeviceRGB` raw pixel data via manual decode to PNG; a few slides bake several photos into one full-slide composite image, cropped apart with `jimp`) — kept in the session scratch dir, not committed, since `.claude/skills/course-content/SKILL.md` already documents the technique. Added 11 new `content-images.ts` entries: p13 climate-zones-by-latitude figure, p14 rainforest canopy, p15 savanna, p17 oasis + steppe, p21 subarctic distribution map, p22 tundra + ice cap, p23 highland — plus wired up two forest photos (p19 deciduous/evergreen) that a prior session had already extracted to disk but never connected to `content-images.ts` (the units they needed didn't exist until this session's Deciduous/Evergreen/Mixed forest split). Every entry verified rendering correctly in a local dev server across all 6 of Chapter 2's climate topics.
- [x] **Completed Chapter 4's photo-extraction track — the whole chapter is now done, 14 new photo entries.** Same extraction technique, applied to `chapter-4.pdf` (43 embedded-image candidates found, many duplicates/decorative-template/already-captured filtered out by visual review). Added: p2 nature tourists, p3 hand-and-mountain, p9 hiker/lake, p10 waterfall, p17 plateau-city + Andean-altiplano highland, p18 sand dunes, p20 sea horizon, p21 lake, and — from a second captioned photo grid found on p24 (a sibling to the already-used Atoll/Island&Cays/Peninsula grid) — Coral reef, Bay, River, and Fiord, all unambiguously captioned. Also created a new content unit, **"Mount Kinabalu mountain tourism"** (`case_study`, p14), to host the self-captioning "Low's Peak (4,095.2 m)" summit-signboard photo the audit flagged as a significant unclaimed asset (p13/p14 were previously two photo-only slides with no content unit at all). **Bonus correction found during extraction:** the actual p20 "largest bodies of water" source table reads Pacific/Atlantic/Indian/**Arctic**/Arabian Sea/South China Sea/Caribbean Sea/**Mediterranean Sea**/**Bering Sea**/Bay of Bengal — the audit's manual transcription (and this session's earlier DB entry, written before the table image was directly viewed) had wrongly included "Southern Ocean" and "Coral Sea," which are not in the source table at all. Fixed the `content_units` row directly (id `0b0aa4a1`) with the correct names and exact sq-mi/km figures from the table. DB grew from 131 → 132 published content units (the new Kinabalu unit). All 14 photo entries and the correction verified rendering correctly in a local dev server.

### 2026-08-12

- [x] **Completed Chapter 1's photo-extraction track (Codex, Claude-style review/execution/independent verification workflow).** Added seven real source-slide photos to `content-images.ts`: Topography (p4), why geography matters to tourism (p6), Leisure (p7), Recreation (p8), Push and pull relationship (p17), Push factors in generating areas (p18), and Pull factors in destination areas (p19). Two Terra execution passes extracted only source JPEG objects in separate page ranges; a Sol review pass provided independent selection review; Codex visually inspected every final asset and consolidated the map. Existing p13/p14/p15/p30 Chapter 1 entries were retained. Weak-match, decorative, vector, duplicate, and already-diagrammed assets were intentionally excluded. This completes the Chapter 1 photo track and therefore the photo-extraction portion of the content-depth/photo audit build/fix pass.
- [x] **Restored visible in-topic navigation on small screens (Codex).** The topic list had been changed to a closed-by-default native `<details>` disclosure, which hid every sibling topic link after a learner opened a topic. It now renders open by default, so subtopic links remain visible and still may be collapsed manually. Added a `TopicList` regression assertion for the `open` state.
- [x] **Completed independent production UI/UX review and follow-up fixes (Codex).** Chrome DevTools reviewed the deployed home page; representative CH1, CH2, CH3, and CH4 topic shapes; the Tutor’s grounded response/citation flow; the CH99 empty state; and mobile topic navigation. No console errors or failed document/fetch requests were observed. Fixed the selected-subtopic state so it has a persistent tinted background, bold label, and teal marker; fixed compact-section heading order; and aligned citation-link accessible names with their visible source text. Production Lighthouse snapshot audits passed at 100 for accessibility, best practices, SEO, and agentic browsing on both desktop and mobile. Shipped in `ea4b81e` and `2881c68`.

### 2026-08-16

- [x] **UI-only onboarding and navigation batch (published to production through PR #1).** Added a consistent global header with Course/Guide/Full exam routes and a keyboard skip link; added the `/about` study guide; added landing-page Start learning / Ask the tutor actions and a Read → Ask → Practise orientation; made long CH1/CH2 topic menus compact on small screens while keeping the selected topic visible; added a direct mobile Ask tutor jump; and added contextual return/retry actions to assessments.
- [x] **Assessment accessibility correction.** Replaced an invalid `aria-label` on the visual progress `<div>` with real `progressbar` semantics and value attributes. The local mobile assessment Lighthouse snapshot improved from accessibility 95 / agentic browsing 0 to 100 / 100, with best practices and SEO remaining 100.
- [x] **Clean-GitHub-checkout validation repairs.** Added a Vitest-only `server-only` marker alias, removed JSX construction from the assessment data-fetch `try/catch`, and replaced the generated-only root `LayoutProps` dependency with an explicit children type. A clean checkout now passes 61 web tests, ESLint, TypeScript, and the Next.js production build.
- [x] **Naturalized the learner-facing assessment language.** Reviewed all 208 generated questions and removed wording that exposed the internal content pipeline, including “in the course,” “course item,” “course material,” and draft/publishing references. Rewrote 140 question stems, repaired 34 Chapter 2 option sets, normalized answer explanations and written-answer criteria, and added display-time MCQ option shuffling so the correct answer is not consistently first. The exact, transactional database migration retains source links and answer keys while supporting safe preflight checks.
- [x] **Built the flashcard study mode.** `/flashcards` now turns the 97 published definitions and 6 key takeaways into 103 source-faithful cards without generating new facts. Learners can filter by chapter and topic, reveal answers, shuffle a deck, mark cards as **Got it** or **Review again**, and repeat only the marked cards. Every answer retains its exact source citation and a link back to the related learning note. Flashcards are linked from the global header, homepage, study guide, chapter cards, and individual topic pages.
- [ ] **Anonymous helpfulness feedback.** The table exists, but this is not UI-only: it still needs a secure server/RPC write path plus abuse-conscious validation before learner controls can be enabled.
- [ ] **Manual theme control.** OS-level light/dark preference works; a user-controlled override is a separate UI enhancement.

**Filtered next UI work after this batch:** the blueprint mascot decision, tutor conversation history, source-image display inside tutor answers, and a manual theme control can be built in the frontend. Category restructuring, SDG/suggested-reading enrichment, complete per-topic example coverage, downloadable source PDFs, and feedback submission are not UI-only and stay open pending content, policy, or backend work.

### 2026-08-17

- [x] **Added learner accounts, roles, and the two role dashboards** (branch `agent/learner-accounts`, 4 commits). Two seeded Supabase accounts — `lecturer`/`lecturer` and `student`/`student`. Supabase Auth requires an email, so the login form maps username to `<username>@tgtutor.local`; learners type only the username. The accounts were created directly in `auth.users` with matching `auth.identities` rows, because the service-role key is not persisted on this machine by policy; a trigger on `auth.users` creates the `public.profiles` row from user metadata, so future accounts stay consistent. The course materials, flashcards, and practice assessments remain open without an account — signing in adds persistence and the dashboards, it does not gate the learning content.
- [x] **Enforced role separation in Postgres, not in React** (`202608170001_learner_accounts.sql`). Signing in makes a visitor the `authenticated` role, which previously had no access to the question bank at all. The migration grants `quiz_questions` / `quiz_question_options` / `quiz_marking_criteria` to `authenticated` but gates every policy on the lecturer role, so a student still reads **zero** answer keys and **zero** rubrics. This was the one place the change could have widened the trust boundary, and it was verified by probing the database as each role *before any UI existed*, then again by e2e test. Two asymmetries are deliberate: lecturers have **no policy at all** on `bookmarks` or `topic_progress` (so a learner's saved material and reading history cannot leak through a dashboard bug), and students have **no insert policy** on `assessment_attempts` (so a score cannot be self-reported). A mutual-recursion bug between the `classrooms` and `classroom_members` policies was found and fixed during probing — both lookups now go through `SECURITY DEFINER` helpers.
- [x] **Student dashboard.** Overview (best vs latest score, topics studied out of 23, weakest topics deep-linked to the exact chapter/topic, resume-where-you-left-off), Results (every attempt grouped by assessment, best vs latest, retake), a full attempt replay with answer schemes and **Which to refer** citations, Studied, and Saved material. Best-vs-latest is separated deliberately: a retake that goes badly must not erase the learner's best result.
- [x] **Lecturer dashboard.** Class stats, roster, per-student results with per-topic accuracy (and **no** bookmarks or reading history, enforced in RLS), question bank with chapter/topic/type/status filters and full CRUD, and the **draft-approval queue** — the workflow `AGENTS.md` said had to exist before AI drafts reached learners. All 208 generated questions are still `draft`; the database refuses to approve any question that is not complete and traceable to a published, cited unit, and the trigger's message is surfaced to the lecturer verbatim rather than being duplicated in app code.
- [x] **Persisted learner activity.** Reading history is recorded from a client effect rather than during render, so a prefetch or replayed render does not count as reading. Saved material covers both course units and flashcards (marking a card for review saves it; "Got it" clears it). The save control is deliberately **not** optimistic — an e2e test caught the button reporting success while the row never reached the database, because navigating away cancels the transition. It now changes only after the round trip completes.
- [x] **Moved assessment marking off the browser** (`/api/attempts`). The client previously totalled its own score from per-question responses — fine for display, not something a stored result can be based on. One request now marks the whole paper server-side; the request carries no marks at all, and a unit test asserts both that the payload contains none and that the rendered score comes from the server.
- [ ] **Verify the assessment submit-and-record round trip live.** Blocked on `SUPABASE_SERVICE_ROLE_KEY`, which is not on this machine and which `vercel env pull` returns as the literal string `"[SENSITIVE]"`. Code typechecks, builds, and passes unit tests; the live round trip is unverified. To finish: put the real key in `web/.env.local`, restart `next dev`, run `BASE_URL=http://localhost:3000 npx playwright test e2e/assessment.spec.ts`.
- [ ] **Phase 2, not started: the AI PDF-to-questions importer** for the lecturer. Needs a Node-side PDF text extractor (`unpdf` is the serverless-friendly pick; the repo's Python/`pymupdf` tooling will not run on Vercel). Extracted questions must land as `draft` and be given a topic and a published source unit before approval, per the source-fidelity rule.
- [ ] **Lecturer features scoped but not built:** item analysis (% correct per question from real attempt data), manual mark override on auto-graded written answers, CSV export of the roster, and a review queue surfacing the source defects this checklist flags but deliberately does not fix.

**Working-directory change (2026-08-17):** build and test from the local clone at `C:\Users\User\dev\tourism-geography-tutor`, not the Google Drive checkout. Drive's file provider truncates and fails to materialise `node_modules` — 758 zero-byte `package.json` files and 13,022 zero-byte `.js` files out of 28,115, against zero on local disk; a single small `npm install` there took over five minutes and left 120 of 130 files empty. The Drive copy still holds `data/course-materials/` and the handoff package.

**Running the e2e suite (2026-08-17):** `npm run test:e2e` starts its own server with the Supabase env deliberately blanked, which is what the pre-existing empty-state spec needs; the auth, dashboard, learner-activity, and assessment specs skip themselves there. To run those, point the suite at a configured server: `BASE_URL=http://localhost:3000 npx playwright test`.

### 2026-08-18

- [x] **Deployed learner accounts to production** (PR #6 merged to `main`). Live at https://tourism-geography-tutor.vercel.app/login with `lecturer`/`lecturer` and `student`/`student`.
- [x] **Verified the assessment submit-and-record round trip** — the item left open yesterday. It could not be checked locally because `SUPABASE_SERVICE_ROLE_KEY` is deliberately never stored on this machine, and `vercel env pull` returns it as the literal string `"[SENSITIVE]"`. Production has the real key, so verification ran there: a full course exam submitted as `student` stored 16 answers, each with its answer scheme, citation, and topic, and all 6 written answers marked per-criterion. Confirmed by querying the database directly rather than reading the screen, per the standing workflow rule. The synthetic attempt was then deleted so both demo accounts start empty.
- [x] **Ran the full live verification suite against production** — 12 auth and dashboard e2e tests plus the assessment round trip, via `BASE_URL=https://tourism-geography-tutor.vercel.app npx playwright test`. All passed.
- [ ] **Investigate the GitHub → Vercel production hook.** Merging PR #6 to `main` did **not** trigger a Production deployment; only the branch push produced a Preview. Worked around with `npx vercel promote <preview-url>`, after confirming the merge commit's tree hash matched the tested build exactly so promotion was equivalent to building `main`. The handoff doc previously stated "push to `main` = production deploy, no manual step needed" — that was not true on this occasion. **If a future merge appears not to deploy, check `npx vercel ls` before assuming the build failed.**

### 2026-08-20

- [x] **Refreshed the handoff doc's "Right now" section for a tool handoff.** No code or content changed. Verified the starting state before writing it: `main` clean and in sync with `origin/main` at `6bd5d64`, `agent/learner-accounts` merged with no unpushed work, and production returning 200 on `/`, `/login`, and `/flashcards`. The section now states plainly that nothing is in flight, names the read-first order for a new agent (`docs/handoff.md` → `docs/checklist.md` → `AGENTS.md` → `.claude/skills/course-content/SKILL.md`), restates the two rules that cause the most damage when missed (never invent course content; insert-then-delete on `content_units`), records that `SUPABASE_SERVICE_ROLE_KEY` is never stored on this machine, and lists the five open items in priority order with the Vercel production-hook investigation first. The 2026-08-18 release notes were kept and demoted below it.

### 2026-08-21

- [x] **Re-audited Chapter 1 against the source PDF and found a content-fidelity incident.** Two independent scanners (one reading all 33 rendered slides plus the text layer, one reading the live database, two passes each), a main-thread comparison re-verified against the raw text layer, and an independent Opus verification pass that confirmed all nine findings and caught a third fabrication. Findings: `docs/ch1-pdf-vs-db-discrepancies-2026-08-21.md`. Plan: `docs/content-fidelity-remediation-plan-2026-08-21.md`. Live resume point: `docs/remediation-progress-2026-08-21.md`.
  - **Root cause:** `docs/content-depth-photo-audit-2026-08-09.md`'s `Source:` strings are paraphrases, not verbatim transcriptions. The 2026-08-11 body-enrichment pass rewrote 41 bodies across all four chapters from *that document* rather than from the PDFs, so wherever its quotation was wrong the database encoded the error. Because that pass was adding depth, the errors are additions — they read richer and more authoritative than the thin-but-true text they replaced.
  - **Three Chapter 1 units state things that are on no slide:** `90510d10` (p6 — invents "transportation" and narrows "job opportunities" to "career opportunities in tourism"); `10f721f6` (p29 — two sentences attributed to the slide that do not appear on it, while the slide's real bullets 2–3 are absent); `571f3d66` (p30 — "by Muslims", which the deck's abridged Battour & Ismail abstract does not contain; the published paper does, but the slide governs).
  - **One draft quiz question can mark a learner wrong for the source-correct answer** — its criterion accepts `career opportunities in tourism` / `tourism careers` but not "job opportunities", which is what the slide actually says. The narrowing is in three fields of that row: the stem, `subjective_answer_scheme`, and the criterion.
  - **Coverage itself is sound** — 40 published CH1 units, 9 topics, every one cited, no orphans, no drafts. Slides 1, 2, 10, 12, 16 genuinely carry no teachable content (confirmed by image); p24 is a six-tile section index.
- [x] **Marked the 2026-08-09 audit document untrusted as a source of quotations** with a header warning at the top of the file. Its *findings* (which pages are shallow, where photos are) remain useful as leads; only its quotations are unreliable. This was done before any fix work, because the document is an input to every remaining step and to Chapters 2–4.
- [ ] **Chapters 2, 3 and 4 have not been re-verified against their PDFs.** The same 2026-08-11 pass rewrote bodies there from the same contaminated document. Measured CH1 defect rate was 3 fabrications across the ~17 pages that pass touched, and nothing about the failure mode is chapter-specific. CH3 and CH4 are also far more OCR-dependent than CH1 (6 of CH3's 11 slides and 10 of CH4's 27 have no text layer), and CH4 cites nothing past p22 despite having 27 slides.
- [ ] **Client decision needed: the silent-correction policy.** Chapter 1 currently handles source typos *both ways* — p3 ("nature circulation" → "nature and circulation"), p5 (three defects rewritten and the sentence re-parsed) and p23 ("spends" → "spent") are silently corrected, while p6 "INRELATED", p26 "determine" and p33 "involve" are left alone. The standing rule says flag, don't fix. One ruling is needed, then uniform application — including to the Chapter 3 "nine planets" / "Hindi" / "Artic" decisions already made on the other basis.
- [ ] **Client decision needed: p26's five tourism types.** Rural, Urban, Heritage, Cultural and Eco-Tourism sit in one merged unit. The one-entity-one-unit rule would normally demand five units, but the deck names all five and defines none, so splitting would require inventing definitions. Recommendation: leave merged, flag. Same posture as Chapter 4's "valley" and "beach" structural gaps.
- [x] **Re-scanned Chapter 4 against its PDF (same two-scanner + Opus-verifier method).** CH4 is materially healthier than CH1 but **not clean**: two fabrications and one distorted figure were found and fixed, all verified against production.
  - `Desert` (p18) asserted "in which living conditions are hostile for plant and animal life" — the next sentence of the Wikipedia desert lede, on no slide. The slide's definition ends at 250 mm/year.
  - `Mount Kinabalu mountain tourism` (p14) asserted "in Sabah, Malaysia" and "via ferrata"; neither appears anywhere in the deck, and the portaledge/cable material is on p13, which the unit does not cite. p14 has no text layer at all — it is a photo collage plus a summit signboard.
  - `Tourism and mountains` (p12) said "an estimated 50 million", turning the slide's "**More than** 50 million people visit mountains each year" from a floor into a point estimate.
  - **Method note:** a main-thread spot-check of pp.19–22 found no fabrication and was reported as "CH4 looks clean". The Opus sweep of all 40 units refuted that — both fabrications sat outside the sampled range. Sampling is not sufficient evidence for this defect class.
- [x] **Added the missing `Continents` content unit** (p22, "Main land masses of the earth"). Page 22's glossary defines 15 terms; 14 had units and this one did not. `Continental landmasses` (p19) is a highest-point table, not a definition. Inserted as draft → source_references → published, per the standing order. Also gives p24's previously stranded Continents photo a unit to host it.
- [x] **Corrected four mis-cited content images.** The Coral reef, Bay, River and Fiord photos are on **page 25**, not page 24 — the deck has two sibling captioned photo grids. Fixed `pageOrSlide`, the rendered caption, and the filenames (`ch4-p24-*` → `ch4-p25-*`, files renamed via `git mv`). Typecheck clean, 84/84 web tests pass.
  - **Status/Update (2026-08-21):** this corrects the page attribution stated in the 2026-08-11 Chapter 4 photo-extraction entry above, which recorded that grid as being on p24. The four photos themselves were correctly identified and correctly attached to their units; only the page number was wrong.
- [ ] **Client decision needed (extends the Chapter 1 silent-correction question to CH4).** Page 22's 15-row glossary has been silently rewritten out of its broken source grammar, with three semantic additions — Coral reef "formed by", Cays "made up largely of", Peninsula "almost completely" — plus silent typo corrections elsewhere ("eighteen century"→"eighteenth", "Columbia"→"Colombia", "km"→"km²"). Same class as the CH1 ruling; needs one decision applied uniformly across all four chapters.
- [ ] **Flagged, not fixed (source defects, per the source-fidelity rule).** New in this pass: p3 "RESOUCES" and "eighteen century"; p4 "MULTIPLES USE" and "mangement"; p6 "PYHSICAL … WORD"; p15 "HIGHEST MOUNTAIN"; p16 "Columbia" for Colombia and the Taurus Mountains grouped under Europe; p17 "Andes Mountain which called"; p22's pervasive non-standard glossary grammar; p23 labelling two different features "River" (call-outs 10 and 13); p27 "HOSPTITALITI"; and a p1/p27 byline mismatch ("BINITI" vs "BINTI"). **New structural gap:** p7's third classification bullet has no category label, unlike the other three.
- [ ] **Possible information loss in the source deck itself.** The tables on p15 (mountains), p19 (island peaks), p20 (bodies of water) and p21 (river list) appear hard-cropped at their bottom edge — the deck author seems to have pasted truncated screenshots. The database is faithful to what the slides show, which is all it can be; flagging so the lecturer can supply the full tables if they exist.
- [ ] **Chapters 2 and 3 still not re-verified.** CH2: 41 units, 23 slides, 6 with no text layer. CH3: 11 units, 11 slides, **6 of 11** with no text layer.
- [x] **Re-scanned Chapter 2 against its PDF.** One fabrication, three unsupported insertions, one wrong-citation pair and one uncaptured source table found and fixed; all verified live.
  - **Fabrication:** `Antarctica` (p6) said "no permanent **countries or residents**". The slide says only "It does not have any countries but it contains several research laboratories." p6 has **zero embedded images**, so its text layer is provably complete — the words "residents" and "permanent" appear nowhere in the deck. The same edit also restored the slide's "windiest and driest **place on Earth**", which the database had silently changed to "continent".
  - **Unsupported insertions removed:** `Pacific Ocean` (p8) claimed "largest **and deepest** ocean" — the slide says it *has* the deepest known point, a different claim; `Evergreen forest` (p19) added "year-round" to the slide's "keep their leaves"; `Ice cap climate` (p22) added "in places" and hardened the slide's "can be more than 2 miles thick".
  - **Wrong citations fixed structurally.** `Marine west coast climate` and `Humid continental climate` each carried a second sentence sourced from the **embedded Mid-Latitude Climates screenshot table on p18**, while citing p20. The two units were trimmed to their p20 content and the p18 table was given its own unit, **`Mid-latitude climates compared`** — the same whole-table treatment used for Chapter 4's "Largest deserts". Nothing was lost; everything is now traceable to the page it cites. Also restores "and northwestern Europe", which the old body had dropped.
  - **Merged-entity candidates confirmed as FALSE POSITIVES.** All 7 continent and all 5 ocean units exist; the list and synthesis units were deliberately retained per the August entries above. **No split is outstanding — do not re-split them.**
  - **Coverage is complete:** the only two uncited pages are p1 (cover) and p11 (section divider).
- [x] **Recorded the extraction trap that caused a near-miss.** The main thread called the two p18-sourced sentences fabrications and was about to delete legitimate course content. A text-layer sweep for "coniferous"/"korea"/"japan" had returned nothing, which was read as proof the text was absent from the deck — but **p18 has a text layer that silently omits its embedded 525×426 screenshot.** The blind spot is not the image-only pages (those get read carefully precisely because they look empty); it is pages that *appear* covered by their text layer while carrying content only in an embedded raster. **Standing rule: enumerate embedded image XObjects per page and visually read any page carrying a large one, regardless of text-layer status.** In CH2 that means pp.12, 13, 14, 16, 17, 18, 19, 20, 21, 22, 23 — five of which have a non-empty text layer.
- [ ] **Client decision (now spans three chapters): the silent-correction policy.** CH2 adds 8 instances — 4 mechanical (`loose`→`lose`, `immposible`→`impossible`, `grasess and few tree`→`grasses and few trees`, `Scandinavia`→`Scandinavian`) and 4 silent corrections of substantive source *claims* (the Nile superlative hedged; Antarctica "place on Earth"→"continent", now reverted; treeline "nothing can grow"→"trees"; "most inaccessible"→"least accessible"). The last four matter more than typos: they change what the course asserts.
- [ ] **Flagged, not fixed (CH2 source defects).** A grey redaction bar obscures the screenshot headers on pp.19–23. p18's Mid-Latitude table shows only 2 of the deck's 4 mid-latitude sub-types. p12 shows two different classification systems side by side — the 5-type climate taxonomy and a 9-key biome legend (Tropical Rainforest, Grasslands, Desert, Deciduous, Tundra, Coniferous Forest/Taiga, Chaparral, Savanna, Alpine) — unreconciled, with "Desert" and "Tundra" in both. The database has **not** conflated them; no unit uses Chaparral/Alpine/Grasslands/Taiga. p12's biome legend has no unit of its own.
- [ ] **Metadata gap worth closing: all 41 CH2 `source_references.excerpt` values are NULL** — as are CH1's and CH4's. An excerpt holding the actual slide text is precisely the guard that would have made the paraphrase-as-quotation incident detectable at review time instead of a year later. Worth backfilling as part of the remediation.

## 2026-08-25 — Pedagogical review workstream (separate from factual accuracy)

- [x] **Vendored 9 evidence-based pedagogy skills and built a read-only reviewer agent.** From `GarethManning/education-agent-skills` (165 skills, CC BY-SA 4.0), nested at `.claude/skills/pedagogy/` so they stay inert until loaded by name. The `course-pedagogy-reviewer` agent (Opus, `Read/Grep/Glob/Bash`, GET-only against Supabase) reviews teaching quality and writes a report; it never writes content. Its governing rule is the **Layer 1 / Layer 2 split**: source-derived prose can only be flagged, while the scaffolding around it — ordering, splits, visual pairing, question design, marking schemes, summaries — is open to proposal. Attribution, per-skill rationale and rejected candidates in `.claude/skills/pedagogy/ATTRIBUTION.md`.
  - **Blocked and worth unblocking:** `curriculum-alignment/coverage-audit` is the strongest skill in the library for this project and cannot run. It needs the DTM10333 syllabus / course learning outcomes as its framework input, and `data/course-materials/fyp cb.pdf` is a single image-only page with no text layer. **Ask the client for a machine-readable syllabus.** Until then nothing verifies that the app actually covers what the course promises.
- [x] **First review pass: all 134 published units.** Report at `docs/pedagogy-review-content-units-2026-08-25.md` — 9 findings, labelled 14 `SAFE-TO-APPLY` / 5 `NEEDS-CLIENT-RULING` / 3 `BLOCKED-BY-FIDELITY`. Three headline claims were independently re-verified against the live DB and the code before acting.
- [x] **Gave seven flattened-table units their source tables back.** Seven units transcribe a source *table* into one run-on sentence; the worst, `10376fc0` *World's highest mountains*, is **71 words in a single sentence** carrying six entities by three attributes. Extracted the tables at native resolution from the embedded PDF images and wired them into `content-images.ts` with full row-by-row alt text. **No stored prose changed.** p19 held two stacked tables in one image and was cropped into its two units; CH2 p18's table was cropped from the rendered page clear of the screenshot's UI chrome; the mountain-ranges world map was re-encoded to JPEG (1.7 MB → 301 KB).
  - This also restores data the prose had dropped: `0b0aa4a1` reports only the first and last of ten area values, and `10376fc0` drops the rank column that is the point of a ranked table.
  - **Correction to the review:** it listed `2fe03acb` (p7) among the table units. It is not one — p7's only image is a decorative full-bleed waterfall photo, and per Mayer's coherence principle decorative imagery harms learning, so it got nothing.
  - **Confirms the 2026-08-21 entry above** about hard-cropped source tables: p19's island-peaks table is visibly cut off mid-row-six in the deck itself. Also worth recording that the deck's own figures on pp.15, 18, 19 and 20 are **Wikipedia screenshots**, blue links and footnote markers included. They ship as the deck drew them; flagging for the accuracy workstream, not adjudicated here.
- [x] **Unit bodies now keep their line breaks.** `content-unit.tsx` interpolated `body` into five `<p>` elements with no `whitespace-pre-wrap`, so any newline collapsed — while the bookmarks page already set it, meaning the same string rendered with structure there and without it on the page learners study from. No behaviour change for the 134 current bodies; it converts "give a body structure" from impossible into an option.
- [x] **The visual maps now fail loud.** Both `contentImages` and `topicDiagrams` are hardcoded literals keyed on database UUIDs, and `content-unit.tsx` returns `null` on a miss — so a re-imported unit (exactly what an insert-then-delete split produces) loses its image with no error and no test failure. New test in `web/src/lib/course-brain/visual-maps.test.ts` asserts every key resolves to a live published row and every `src` exists on disk. Offline half always runs; the live half skips cleanly without credentials. **Added before, not after, acting on any split recommendation.** 88/88 tests pass.
- [x] **Applied and verified: deterministic ordering for 18 units.** `repository.ts` orders by `created_at` with no tiebreaker and the importer commits in one transaction, so 18 units across 5 topics share an identical timestamp and come back in plan-dependent order — while `group-units.ts` promotes `units[0]` to the page lead. **In four of the five topics every unit is tied, including CH4's opening topic, so which sentence introduces that chapter is currently arbitrary.** Full plan, reasoning per position and verification results: `docs/unit-ordering-fix-plan-2026-08-25.md`. **Applied 2026-08-25**; 18 rows written, each guarded on the old timestamp and asserted to affect exactly one row. Verified by re-reading every topic in the app's own order and by fetching the deployed page: CH4's opening topic now leads with *Nature-based tourism and natural resources*. **Course-wide ties: 18 → 0.** Gotcha worth remembering: `+00:00` in a PostgREST `eq.` filter must be percent-encoded or Postgres rejects it as `22007`.
  - **Durable fix, separate decision:** a real `display_order` column would end this class of bug rather than encoding sequence in a timestamp the next bulk import will re-tie.
- [x] **Split CH4's 20-unit *Water environments* topic and relocated three misfiled land units.** It was ten times the smallest topic in the app, one undifferentiated glossary scroll with no internal landmark, and *Continents*, *Continental landmasses* and *Highest island peaks* are land topics that were filed under water. Applied to production 2026-08-25 and verified.
  - **Three land units moved to *Natural landscapes and landforms*** (12 -> 15 units), slotted in source-page order after *Largest deserts* (p18): *Continental landmasses* (p19), *Highest island peaks* (p19), *Continents* (p22). They join the mountain and highest-point tables they belong with. This is the mirror of the 2026-08-09 pass that moved *Island* out of landscapes into water.
  - **The remaining 17 split three ways** along a distinction the content already makes: **Seas and oceans** (5: Ocean, Sea, Largest bodies of water by area, Gulf, Bay), **Islands and coastal features** (6: Island, Cays, Atoll, Peninsula, Coral reef, Lagoon), **Inland and glacial water** (6: River, Lake, Fiord, Springs, Waterfall, Glacier). Words unchanged; only `topic_id`, `created_at` and topic names moved.
  - **The existing topic row was renamed rather than replaced.** `c5a8827e` became *Islands and coastal features* and kept its p23 hydrosphere/lithosphere floor-plan diagram, whose labels concentrate on exactly that group. Renaming instead of delete-and-recreate is why the diagram binding survived — a new topic id would have orphaned it silently, which is the failure mode the new integrity test now catches. The two new topics have no diagram yet.
  - **Verified:** CH4 still totals 41 units and the course still totals 134; zero orphans; zero units anywhere sharing a `created_at` with a topic-mate; all three topics fetched from production render their intended lead unit.
- [x] **Added the project's first constructed diagram: the CH1 *Forms of tourism* 2x2 matrix.** Domestic, international, inbound and outbound are not four independent definitions but one matrix the prose never draws, and inbound-versus-outbound is precisely the pair learners confuse. The matrix makes the reference frame explicit ("Call it Country A"), and the fourth cell is deliberately empty because no term on the page names it.
  - **Every quoted label is verbatim from the four unit bodies**, all of which cite `chapter-1-candidate-a.pdf` p25. The diagram contributes arrangement, not content. Its caption says so — *"a matrix drawn from the four definitions given on this page"* — because the figcaption template otherwise reads as provenance and would imply the figure was extracted from the slide.
  - Generated by `scripts/build_ch1_forms_of_tourism_diagram.py`, so it can be regenerated and reviewed rather than being an opaque binary. **Note for anyone editing it:** the PDF base-14 fonts cannot encode curly quotes or en/em dashes and pymupdf silently substitutes `?`, so the script asserts its strings are ASCII and that every text box actually fits.
  - **Not live yet** — unlike the database work, this is code, so it needs the branch merged and deployed.
- [x] **Deployed. `main` fast-forwarded to `159740b` and pushed; Vercel built and is serving it.** This closes the gap flagged on 2026-08-21: the branch had been carrying finished work that learners could not see, because database fixes go live at runtime while code fixes need a deploy. 21 commits, spanning both the content-fidelity remediation and the pedagogy pass.
  - Verified live, not assumed: all 8 new figures return 200; the four renamed photos serve at `ch4-p25-*` and the old `ch4-p24-*` names now 404, so the p24→p25 citation correction is finally visible; the *Forms of tourism* matrix renders on CH1 t5 with its "drawn from the four definitions" caption intact; the five CH4 table figures are bound to their units; the three relocated land units appear under *Natural landscapes and landforms*; and `whitespace-pre-wrap` is present in the served markup.
  - `npm run build` was run before merging — typecheck, lint and 88/88 tests all pass, but none of them exercise the Next production build.
  - **`agent/learner-accounts` was deliberately not merged.** It is a separate feature branch and was not part of this work.
- [ ] **Still not executed, needs a decision.**
  - **Topic-level elaboration prompts** (~23, not 134) to raise germane load: 73% of units are `definition` and CH2 is 36 definitions out of 42, so the deck asks for recognition rather than schema construction. Needs a UI slot first.
- [ ] **Client input wanted: the learner reading level.** The review assumed CEFR B1 (FK grade 7-9 comfort band) for DTM10333. CH1 sits at **FK median 15.2 with 52% of units at grade 15+**, is the least illustrated chapter (1 diagram across 9 topics), and comes first. If the real cohort reads at B2+, that finding softens considerably. The actual entry English requirement would settle it.

## 2026-08-26 — Native learning models (CH1) and licensed HD photography (CH2)

Client-directed, both items. Started by Codex, whose session ended mid browser-verification;
picked up, re-verified independently and committed on `agent/native-learning-models`
(`f4637ca`, `614a4a6`).

- [x] **The two CH1 diagrams are now native interface components, not images.** *Push and pull
  factors* and *Forms of tourism* each rendered a flat raster and then repeated the same
  definitions in separate cards below it — split attention in the textbook sense: the learner
  holds the picture in mind while reading the prose that explains it. `topic-learning-model.tsx`
  replaces both with one component per topic, assembled **from the published units themselves**.
  - **No unit wording changed.** The push/pull factor lists are *parsed out of* `c403af77`'s body
    (`Push factors: … Pull factors: …`) rather than retyped, so they cannot drift from the stored
    text; all 17 factors render. Every source unit keeps its `unit-<id>` anchor, its bookmark
    control and its citation, and each model footer prints the source file and page range it was
    assembled from.
  - **Fails safe.** `integratedTopicUnitIds` claims a topic only when *every* required unit id is
    present, and the chapter page filters exactly those ids out of the ordinary sections. A
    re-imported unit with a new UUID therefore falls back to normal cards rather than vanishing —
    the same silent-drop failure mode the visual-map tests were written for. A test covers it.
  - **Two assets were deleted:** `ch1-push-pull-model.jpg` (extracted from p20) and the constructed
    `ch1-forms-of-tourism-matrix.png` with its build script, added only the day before. Both are
    recoverable from history. **Worth a client note:** the deck's own p20 Push-Pull figure is no
    longer shown anywhere; the native table carries the same labels, but if the lecturer wants the
    slide's figure visible it has to come back as a separate unit-level figure.
  - Added `[id^="unit-"]:target` to the locate animation so deep links into a model highlight the
    right block, as they already did for ordinary cards.
- [x] **Five CH2 climate photographs: four upgraded, one gap filled.** Replaces the low-resolution
  crops taken from the deck (276-414px wide) with 1280-1600px concept-bearing scenes: primary
  rainforest at Khao Sok, Serengeti savanna, the Timimoun oasis, Inner Mongolian steppe. *Summer
  monsoon* gains its first image. **The Dry-climate table asset was left untouched, as instructed.**
  - **This is the first content imagery in the project that does not come from the course deck**,
    and it is client-directed, not an agent decision. `ContentImage` gained an `attribution` block
    and the figure now renders creator, source link and licence link under the caption — a CC BY /
    BY-SA obligation, not a nicety. `public/content-images/ATTRIBUTION.md` records the same table.
  - **The type change opened a hole, so it is now tested.** Making `sourceFile`/`pageOrSlide`
    optional to admit non-deck images also permitted an entry with *no* provenance at all. Two new
    tests in `visual-maps.test.ts` require every entry to declare either a course source or a
    complete attribution, and every attributed file to appear in `ATTRIBUTION.md`.
  - **Winter monsoon (`c31bc5c9`) was first left image-less, then given one on the owner's
    instruction.** The objection — a still landscape cannot show air moving off the land, so the
    photo would decorate rather than teach — holds for an image standing alone, but this one does
    not stand alone: it sits directly beneath the summer-monsoon image in the same topic, and the
    pair carries the contrast neither photo carries by itself. Chosen for what it shows: the Nan
    River near its source reduced to a stream across a wide gravel bed, harvested paddy terraces,
    dry haze — Nan Province, Thailand, photographed in December, the dry half of the monsoon year,
    one screen below the Khao Sok rainforest photo. Same country, opposite season. Takeaway,
    CC BY-SA 3.0.
- [x] **Verified independently, not self-reported.** 94/94 tests (including the live Supabase
  referential half — every image key resolves to a published unit), typecheck, lint and the Next
  production build all clean. Both CH2 climate topics fetched at 1440px and 390px: all five images
  200, no failed requests, no horizontal overflow, definitions still visible.
  - **One warning left standing:** Next reports the first climate photo as the LCP element and
    suggests `loading="eager"`. It is a real hint now that these are 1600px files, but `UnitImage`
    has no notion of which unit sits above the fold, so fixing it properly means passing position
    down from the section. Not done; noted.
- [x] **Deployed the same day.** Owner approved; `main` fast-forwarded to `793dcaa` and pushed,
  Vercel rebuilt. Verified against production, not assumed: all six new images 200, the four
  replaced deck crops and `ch1-push-pull-model.jpg` now 404, both native models serve their full
  content (17 push/pull factors, the deliberately empty fourth matrix cell), and every licensed
  figure serves its creator and licence links. No repeat of the 2026-08-21 four-day gap between
  finished code and visible code.
- [ ] **Follow-up worth doing: the LCP warning.** Next asks for `loading="eager"` on the first
  climate photo of each topic. `UnitImage` cannot know which unit is above the fold, so the fix is
  to pass position down from `ContentSection`. Left undone; it matters more now that these files
  are 1600px rather than 300px crops.
- [ ] **Open for the client: should the deck's own p20 Push-Pull figure come back?** The native
  table carries the same labels, but the slide's figure is currently shown nowhere. It would return
  as a unit-level figure on `c403af77`, not as the topic diagram.

## 2026-08-26 (second pass) — the last three CH2 climate topics

Client-directed continuation of the same day's work: HD imagery for Middle latitude, High
latitude and Highland, plus the mid-latitude comparison table merged into the interface and the
section resequenced. Commits `5840eb8`, `0cc4215`.

- [x] **The Mid-Latitude Climates table is now a native table, and it comes last.** The deck ships
  it as a dark, blurred screenshot — two sub-types by four attributes — and the app was rendering
  it **twice**: once as the topic diagram (`diagrams.ts`) and again as the unit's own image. Both
  bindings are gone.
  - **Cells are parsed out of the stored unit body**, not retyped, so the table cannot drift from
    the transcription it renders; the test drives the parser with the exact string the database
    serves, and an unparseable body falls back to prose rather than a broken table.
  - **Axes transposed against the source**: attributes as rows, the two climates as columns.
    Down a column is one climate's profile; across a row is the contrast the table exists to make.
    Column headings are the deck's own ("Latitude Range", "World Location", "Vegetation",
    "Seasons/Rainfall").
  - **Placement was the other half.** The table arrived third in the topic — before Marine west
    coast and Humid continental, the two sub-types it compares, and before the three vegetation
    terms its Vegetation row uses. Models now have a placement: *leading* ones replace a topic
    (Push-Pull, Forms of tourism), *trailing* ones consolidate a topic already read. This one is
    trailing, so the section reads: intro, four sub-types, vegetation vocabulary, comparison.
    **No database write was needed** — pulling the table unit out of the ordinary flow fixed the
    order by itself, which matters because the service-role key is not on disk.
- [x] **Twelve HD photographs across the three topics.** Nine deck crops (170px-525px wide)
  replaced at 1600px; three units that never had an image — **Humid subtropical**, **Mixed
  forest**, **Highland examples** — now have one.
  - Chosen for what each shows, per the vendored `dual-coding-designer` rule that a visual must
    add what words cannot: deciduous shows broad leaves actually turning, evergreen shows needled
    crowns over bare trunks, mixed is shot straight down so both canopies appear at once, tundra is
    treeless where the taiga card above it is not, and highland shows the tree line **as a line**,
    forest below and bare rock above — the one thing that unit's body describes and no previous
    image showed.
  - Two are US public-domain works (Forest Service, NPS), so `licenseUrl` became optional; the
    figure renders the licence as plain text when there is no deed to link to, and the provenance
    test demands the link only for CC licences. `ATTRIBUTION.md` is regenerated from
    `content-images.ts` and now covers 18 images.
- [ ] **Client note: the Subarctic unit lost a map, not just a photo.** Its old image was a
  low-resolution scan of a *distribution map* of the Canadian subarctic. The Siberian taiga photo
  replacing it shows what the climate looks like but not where it is. If the lecturer wants the
  distribution back it should return as its own figure, not as this unit's only image.
- [x] **Verified before committing:** 97/97 tests including the live Supabase referential half,
  typecheck, lint, production build; all three topics fetched at 1440px and 390px with no console
  errors, no failed requests and no horizontal overflow. The native table stays inside its own
  scroll container at 390px and remains legible.
