# Project checklist

This is a living checklist for the Tourism Geography Tutor / Course Brain project. It's grounded in the original blueprint (`fyp cb.pdf`, in the Google Drive folder `Aztech Digital/Tourism Chatbot`) and is meant to be appended to, not rewritten, as new client feedback comes in.

**How to use this doc:**
- Check items off (`- [x]`) as they ship.
- When new client feedback arrives, add a new dated subsection under "Client feedback log" rather than editing prior entries — keep the history intact.

## Blueprint feature checklist

From the original mockup (`fyp cb.pdf`, Figures 3.1–3.8), against the actual state of the codebase:

- [ ] **Homepage/landing with chatbot framing** — partial: `web/src/app/page.tsx` exists but is a plain chapter/topic index, not the blueprint's marketing hero with mascot + "Start Chat"
- [x] **Conversational chatbot Q&A interface** — live (2026-08-08): `TutorPanel` is rendered on the chapter page. `grounding.ts`'s keyword-overlap matcher runs first (free); a DeepSeek fallback (`web/src/lib/tutor/llm-fallback.ts`, `deepseek-v4-flash`) now handles questions it misses — typos, loose phrasing, or questions about topics other than the one currently open. Every answer's citation is a real link back to its source content block, which briefly highlights on arrival.
- [ ] **Topic selection by category** (Tourism Destinations, Climate, Physical Geography, Human Geography, Transportation & Access, Cultural Heritage) — model mismatch: current app organizes content by Chapter → Topic drawn from the actual DTM10333 course PDFs, not this 6-category taxonomy from the mockup. Open decision: keep the course-chapter structure, or reorganize navigation around these 6 categories.
- [ ] **Rich answer format** (definition + bullet points + "Example" + "Importance" + related-SDG badges + "Suggested Reading") — partial (2026-08-08): `ContentUnit` now renders a `content_type` badge and visually separates `example` blocks; all 66 published units were reclassified from a single generic type into 44 definition / 7 example / 6 explanation / 6 key_takeaway / 1 case_study / 2 learning_note. Still missing: "Importance" section, SDG badges, "Suggested Reading".
- [ ] **Learning Materials page** (chapter list with View/Download) — partial: chapters/topics are listed on the home page, but there's no dedicated materials page and no view/download links to source PDFs
- [ ] **Quiz/Self-Assessment** (multi-question flow with progress bar + final score) — partial: `QuizCard` + `/api/quiz/answer` work, but `quiz_questions` has **zero rows** in the DB (verified 2026-08-08) — nothing renders today regardless of the single-question-cap limitation. No multi-question sequence, no score summary.

  - **Update (2026-08-12):** assessment delivery is built: topic quiz (3 MCQ/2 written), chapter mini exam (5/3), and full four-chapter exam (10/6) generate randomized approved-only batches, show a score and answer schemes after submission, and provide **Which to refer** links to the cited source unit. Written answers are graded by the installed Tutor/DeepSeek flow with narrow synonym/typo handling. Draft banks now exist for CH2–CH4 and one CH1 topic, but remain deliberately invisible until a future lecturer approval/login decision; this item is not complete until approved question records exist.

  - **Update (2026-08-12, final):** all four reviewed generated banks are now imported: CH1 53, CH2 75, CH3 30, CH4 50 (208 total). The owner deferred login/lecturer approval and enabled these source-validated `draft` / `deepseek_draft` questions for practice; answer keys/rubrics remain server-only until submission. The final production deployment passed Vercel’s build and TypeScript check.
- [ ] **About/User Guide page** — not built: no `/about` route exists
- [x] **Backing content actually published** — resolved: this item was stale. Verified 2026-08-08 by querying the live DB directly that all 66 content units were already `status: published` — the app was serving real content the whole time, just undifferentiated by type (see "Rich answer format" above).
  - **Update (2026-08-08, later same day):** the original text-layer extraction (`scripts/extract_course_materials.py`) skips image-only/scanned pages (`needs_ocr`), which turned out to be a real, significant gap, not a minor one — e.g. Chapter 2 lists "high latitude" and "highland" as 2 of its 5 major climate types but had **zero** content for either, because those sections were image-heavy slides the text extractor couldn't read. Did a full page-by-page visual read (not OCR — direct vision reading) of all 4 chapter PDFs to find what got missed. Result: DB grew from 66 → 86 published content units, 19 → 21 topics (added "High latitude climate" and "Highland climate" under Chapter 2), and Chapter 2's chapter title was corrected from the placeholder `"CH2"` to its real title, **"World Climate"** (read directly off the deck's title slide). Full scan notes, including what's already covered vs. still-unchecked pages, saved at `data/extracted/vision-scan-2026-08-08.md` so this doesn't need re-scanning from scratch later.
  - **Content-accuracy flag for the client/lecturer:** `chapter-3.pdf` page 2 states there are "nine planet[s]" and names an ocean "Hindi" (likely meant "Indian") and "Artic" (Arctic) — these look like errors/typos in the original slide. Not imported into the DB as fact; flagging rather than silently correcting, since fixing course content is a call for whoever owns the material.
  - **Update (2026-08-09):** enriched Chapter 3's "Hydrosphere"/"Lithosphere" units (`171860d9-25fc-485b-bac2-20cc69fbb0a3` topic) with the 5-ocean/7-continent lists page 2 actually names — used the corrected spellings ("Indian", "Arctic") for the two typo'd ocean names above, matching the spelling already used for Chapter 2's ocean units, rather than reproducing the slide's typos verbatim. Also same page 2: the Equator-diameter fact ("7,926.4 mile[s] from east to west and **7,899.8 mile[s] from north to east**") literally says "north to east," which doesn't describe a coherent diameter — almost certainly meant "north to south" (polar diameter, paired with the equatorial east-west figure). New content unit "The Equator" (id `59312e55-7f96-4ac8-8d00-65ad610a5955`) renders it as "north to south"; flagging the literal source wording here rather than silently carrying the apparent typo into a user-facing fact.

## Client feedback log

### 2026-08-08

- [ ] **Examples grounded in slides** — author `example`-type content units (data/content work during the review step) so each topic's notes surface a slide-sourced example, not just definitions; no new schema needed (the `example` content_type already exists), just content authoring + a UI branch in `ContentUnit` to visually separate example blocks
  - **Status (2026-08-08):** partial. The UI branch shipped, and reclassifying the existing 66 units by type recovered `example` content for 4/19 topics (where the text already named concrete things, e.g. Mount Everest, the Sahara, named plateaus). The other 15 topics still need genuinely new example content authored from the real source slide PDFs — those aren't in this repo checkout (`data/course-materials/` is gitignored and empty), so this is blocked on the client/content-owner supplying them.
  - **Update (2026-08-08, later same day):** got the source PDFs and did a full visual scan (see the "Backing content actually published" update above) — added one genuine new example ("The Prime Meridian as a tourist attraction," Chapter 3) plus 19 other new definition/explanation/key_takeaway units recovered from previously-unreadable slides. Topic coverage for `example`-type content is still incomplete; most topics still have none.
- [ ] **"Let's make a quiz" CTA per topic/chapter** — add a link/button at the end of a topic's content units pointing to that topic's quiz section; can reuse the existing `?topic=<uuid>` query-param routing with an anchor (e.g. `#quiz`) for a same-page jump, or a dedicated `/quiz` route if quiz-only real estate is wanted
  - **Status (2026-08-08):** not started.
- [ ] **Chatbot section for questions** — re-enable `TutorPanel` on the chapter page (component + API route already exist, just unrendered) as a first step; note the blueprint's Figures 3.2/3.4 imply a persistent chat thread, while today's `TutorPanel` is a single-shot Q&A box with no message history — decide whether re-enabling as-is is enough for now or whether a real chat UI is in scope for this round
  - **Status (2026-08-08):** done, and taken further than "re-enable as-is" — see the blueprint checklist's "Conversational chatbot Q&A interface" item above (global search + DeepSeek fallback + clickable/highlighting citations). Still single-shot, no message history — that part of the open decision above remains unaddressed.
- [ ] **Interactive images for definitions** ("define an ocean" → image) — today, diagram images are mapped only per-topic (`diagrams.ts`, keyed by topic UUID, one diagram per topic); showing an image per-definition needs either extending that map's key scheme to content-unit IDs, or a new column/table — this is a data-model extension, not just a UI change
  - **Status (2026-08-08):** not started.
- [ ] **Flashcard section** — net-new feature, no existing flashcard code anywhere in the repo; the `content_type` filter (e.g. `definition`, `key_takeaway`) gives a ready-made dataset to drive flashcards from once content is published; no flip/swipe animation library is installed yet (no framer-motion or similar)
  - **Status (2026-08-08):** not started. The `content_type` dataset this needs now actually exists (see reclassification above), so this is more buildable than when originally written.
- [ ] **Colorful UI** — currently pure Tailwind v4 defaults (`slate-*` grays only, no `@theme` tokens, no design-tokens file); a color pass would start in `web/src/app/globals.css`'s `:root` block, which has no custom tokens defined yet
  - **Status (2026-08-08):** explicitly deferred to backlog per client decision this session — not scoped for the current round.

### 2026-08-09

- [x] **Content oversimplified — individual entities merged into single units** — client caught that Chapter 2's 7 continents and 5 major oceans didn't have individual descriptions (compressed into 3 "Compare X and Y" style units instead of 7 + 5 = 12). Audited and fixed across **all 4 chapters**, not just the reported case: found and split 9 total instances of this pattern (continents/oceans, summer/winter monsoons, sea/ocean, river/lake, domestic/international/inbound/outbound tourism, travel arrangement types, long-haul/short-haul). DB grew from 86 → 111 published content units in this pass. Standards written up in a new `.claude/skills/course-content/SKILL.md` skill so this doesn't recur — the core rule is one distinct named entity gets its own content_unit, even if several share a source page. One genuine content gap (not a merge issue) was flagged, not fixed: chapter-2.pdf p19 names 3 forest types under Humid Subtropical climate with no content_unit yet.
- [x] **Chapter 1 content-quality fixes (Opus review pass)** — chapter title "Chapter 1" → "Tourism Geography" (p1 title slide). Reordered "Geography foundations" topic so both geography definitions are adjacent. Added a missing unit transcribing the Push-Pull Model diagram's own factor lists (previously the diagram had zero supporting text, same class of gap as Chapter 4's mountain-ranges diagram) and added the Williams & Zelinsky (1970) attribution p21 gives for the distance/connectivity unit. Added a missing framing unit for "Measuring tourist flows" (p22's "three types" framing sentence) and aligned three sibling units to the same `content_type` (`definition`) plus enriched two bodies with p23 detail. **Split "Forms and markets of tourism"** (14 units, effectively 3 sub-topics glommed together per its own p24 contents slide) into three new topics — "Forms of tourism," "Tourism markets and products," "Types of travel arrangement" — each reordered to source-page order; added a `case_study` unit for the Tourism Queensland (2002) seniors-market review p31 names, and split a merged youth/gay/grey tourism unit into individual "Youth tourism" and "Gay tourism" units (grey already had its own). Added a closing synthesis unit for "Travel distance" from p33. Fixed one pipeline-voice body.
  - **Found but not acted on (future enrichment, not this pass's scope):** p28/p29 have richer detail than their current bodies capture (common-interest/business traveller specifics); p4/p5 geography-definition bodies drop minor clauses (planets/moons/asteroids; international/multicultural/environmental education).
- [x] **Chapter 2 content-quality fixes (Opus review pass)** — chapter title "World Climate" → "World Geography and Climate" (the chapter covers continents/oceans too, not just climate). Renamed "Continents and basic world geography" → "The seven continents" (the old name was a vestige of the now-fixed ocean-miscategorization bug) and reordered its list unit's body to match the individual continent units' sequence; same fix for "Major oceans," plus moved its synthesis unit ("Continents and their surrounding oceans") to render last instead of third. Added a missing "Climate zones by latitude" opener for "World climate classification" (p13), a missing general lead-in for "High latitude climate" (p21), and a missing "Highland climate examples" unit naming Scandinavia/Norway/Sweden/Iceland (p23). Reordered "Tropical climate" so the two monsoon units sit next to the tropical-humid unit they detail. Rewrote 8 remaining slide-objective-style titles ("Identify the seven continents" → "The seven continents," etc.) to clean noun phrases. Fixed pipeline-voice phrasing in 3 more Chapter 2 units.
- [ ] **Relevant photos on content** — client wants real photos, extracted from the actual source slides (not stock/generic), attached to content. Confirmed technically feasible: real embedded images can be pulled directly out of a PDF's internal structure via `pdf-lib` with no rendering tools needed (this machine has neither `poppler`/`pdftoppm` nor `pip` available). Schema decision made and shipped: `web/src/lib/course-brain/content-images.ts` (`Record<contentUnitId, ContentImage>`, files in `web/public/content-images/`, no Supabase Storage needed) — `ContentUnit` already renders a `<UnitImage>` wherever an entry exists. Still an **empty scaffold**, zero entries populated.
  - **Update (2026-08-10):** client also flagged the content itself as too shallow, not just missing photos ("do for the others as well, not just the part i flagged"). Ran a full page-by-page content-depth-and-photo audit against all 4 source PDFs, saved at `docs/content-depth-photo-audit-2026-08-09.md` — every chapter done, ~40-45 real photo/diagram opportunities identified with page/position/confidence, plus a running list of shallow content_unit bodies, one citation error, and a few genuinely-missing tables. Build/fix pass (extracting the photos, enriching the bodies) not started yet — see `docs/handoff.md` for the next step.
- [x] **Chapter 4 content-quality fixes (Opus review pass)** — verified each finding against `chapter-4.pdf` directly (per-page vision reads, not batched, after a batched multi-page read silently dropped some pages) before writing anything. Fixed: merged two page-4 near-duplicate units into one ("Tourism resources are often shared with other users," second sentence now covers coordination); retyped the mountain-as-barrier unit from `case_study` to `example` (it's one sentence, not a narrative scenario); split "Plateau examples named in the extraction" (pipeline-voice title, and page 17 genuinely gives each plateau its own detail) into 3 individual units — Tibetan/Antarctic/Andean Plateau; added grouped units for the p15 highest-mountains table and the p16 mountain-ranges map (the latter now has diagram support in `diagrams.ts`); added a grouped p19 "Highest island peaks" unit; moved "Island" (p19, "Island and Beaches") from "Natural landscapes and landforms" into "Water environments," and reordered that topic so basic categories (Ocean/Sea/River/Lake/Island) precede the fine-grained glossary terms instead of following them; extracted the p23 labelled floor-plan diagram (`ch4-hydrosphere-lithosphere-floor-plan.png`) via `pdf-lib` raw-stream extraction and wired it into `diagrams.ts` for "Water environments" (previously had no diagram). Checked and found already-clean: p8's attraction-category bare list, and unit `15c93c2f`'s body (no pipeline-voice phrasing present).
  - **Flagged, not acted on:** the p23 floor-plan diagram labels item 13 "River" a second time (item 10 is also "River") next to a lake-shaped blue oval — looks like a mislabelling in the source (probably meant "Lake"); kept the extracted image as-is rather than relabelling. Also found two more tables not yet captured — p19's "Continental landmasses" highest-point table (Everest/Aconcagua/Vinson Massif/Kosciuszko) and p20's sea/ocean ranking-by-area table — genuinely new content but outside this pass's explicit scope, left for a future pass.

### 2026-08-11

- [x] **Started the DB-write half of the content-depth/photo fix pass** (photo extraction was already ~40% done via `content-images.ts`; this is the Supabase side, from `docs/content-depth-photo-audit-2026-08-09.md`'s findings). This session:
  - Fixed the known citation error: Chapter 2 unit `e6f73628` "Tundra climate" was cited to `chapter-2.pdf` p21, corrected to p22 (its content is actually on p22).
  - Added the 3 missing-table content units the audit flagged as having no unit at all: Chapter 4 "Largest deserts" (p18, 9 named deserts by area) and "Continental landmasses" (p19, highest point per continent) under "Natural landscapes and landforms"/"Water environments," and "Largest bodies of water by area" (p20, 10 named oceans/seas) under "Water environments." All published with `source_references` rows, verified live on the deployed site.
  - DB grew from 125 → 128 published content units.
  - **Still open from the audit** (see "Recommended extraction order" section of the audit doc and the standing next-step list in `docs/handoff.md`): the larger body-enrichment pass — several dozen `content_units.body` rows across all 4 chapters flagged **SHALLOW** (missing named examples, numbers, or sub-details their source page actually gives) — has not been started. Structural gaps (Chapter 4's "valley" and "beach" headings with no source content) remain flagged-only, per the source-fidelity rule — not fixed, not invented.
- [x] **Completed the body-enrichment pass** — every **SHALLOW** finding in `docs/content-depth-photo-audit-2026-08-09.md` fixed, all 4 chapters. 41 `content_units.body` rows rewritten to restore dropped named examples, figures, and sub-facts (source-faithful — nothing invented; a few previously-invented clauses that weren't on the actual slide were trimmed instead, e.g. Chapter 2's Subarctic-climate "boreal forest," Australia's "sometimes called an island continent," Marine-west-coast's invented causal clause, and North America's misattributed Isthmus-of-Panama sentence which actually belongs to South America). Also split the client's originally-flagged case — Chapter 2's Humid Subtropical Climate's three named forest types — into their own units (**Deciduous forest**, **Evergreen forest**, **Mixed forest**), per the project's one-entity-one-unit rule. DB grew from 128 → 131 published content units. Spot-checked multiple chapters live (including the two worst offenders, Chapter 1's "Tourist destinations" and "Business and professional tourism") — all render correctly.
  - **Flagged, not fixed, per the source-fidelity rule:** two more structural gaps (Chapter 4's "beach" heading on p19, no beach content anywhere, same class as p18's already-known "valley" gap) and two more source defects — Chapter 4 p21's heading reads "G. SEA AND OCEANS" but the slide is entirely about rivers and lakes (p20 is the real sea/oceans page); Chapter 1 p11's text begins mid-sentence ("…the human activities that involved the tourism can be spatial…"), missing its own opening words, and p10 (a pure section divider) doesn't carry the missing half.
  - **Not done this pass, left for later:** a handful of very-low-value missing items the audit explicitly deprioritized — Chapter 4 p21's alphabetical fragment of North American river names, and Chapter 4 p17's three plateau photos (ambiguous which plateau each depicts). Chapter 4's Mount Kinabalu case-study unit (p13/p14, currently photo-only, no content unit at all) is content creation tied to the photo pass, not a shallow-body fix — deferred to the photo-extraction track.
- [x] **Completed Chapter 2's photo-extraction track — the whole chapter is now done.** Wrote a reusable `pdf-lib`-based extraction script (walks embedded image XObjects, handles both `DCTDecode` JPEGs directly and `FlateDecode`/`DeviceRGB` raw pixel data via manual decode to PNG; a few slides bake several photos into one full-slide composite image, cropped apart with `jimp`) — kept in the session scratch dir, not committed, since `.claude/skills/course-content/SKILL.md` already documents the technique. Added 11 new `content-images.ts` entries: p13 climate-zones-by-latitude figure, p14 rainforest canopy, p15 savanna, p17 oasis + steppe, p21 subarctic distribution map, p22 tundra + ice cap, p23 highland — plus wired up two forest photos (p19 deciduous/evergreen) that a prior session had already extracted to disk but never connected to `content-images.ts` (the units they needed didn't exist until this session's Deciduous/Evergreen/Mixed forest split). Every entry verified rendering correctly in a local dev server across all 6 of Chapter 2's climate topics.
- [x] **Completed Chapter 4's photo-extraction track — the whole chapter is now done, 14 new photo entries.** Same extraction technique, applied to `chapter-4.pdf` (43 embedded-image candidates found, many duplicates/decorative-template/already-captured filtered out by visual review). Added: p2 nature tourists, p3 hand-and-mountain, p9 hiker/lake, p10 waterfall, p17 plateau-city + Andean-altiplano highland, p18 sand dunes, p20 sea horizon, p21 lake, and — from a second captioned photo grid found on p24 (a sibling to the already-used Atoll/Island&Cays/Peninsula grid) — Coral reef, Bay, River, and Fiord, all unambiguously captioned. Also created a new content unit, **"Mount Kinabalu mountain tourism"** (`case_study`, p14), to host the self-captioning "Low's Peak (4,095.2 m)" summit-signboard photo the audit flagged as a significant unclaimed asset (p13/p14 were previously two photo-only slides with no content unit at all). **Bonus correction found during extraction:** the actual p20 "largest bodies of water" source table reads Pacific/Atlantic/Indian/**Arctic**/Arabian Sea/South China Sea/Caribbean Sea/**Mediterranean Sea**/**Bering Sea**/Bay of Bengal — the audit's manual transcription (and this session's earlier DB entry, written before the table image was directly viewed) had wrongly included "Southern Ocean" and "Coral Sea," which are not in the source table at all. Fixed the `content_units` row directly (id `0b0aa4a1`) with the correct names and exact sq-mi/km figures from the table. DB grew from 131 → 132 published content units (the new Kinabalu unit). All 14 photo entries and the correction verified rendering correctly in a local dev server.

### 2026-08-12

- [x] **Completed Chapter 1's photo-extraction track (Codex, Claude-style review/execution/independent verification workflow).** Added seven real source-slide photos to `content-images.ts`: Topography (p4), why geography matters to tourism (p6), Leisure (p7), Recreation (p8), Push and pull relationship (p17), Push factors in generating areas (p18), and Pull factors in destination areas (p19). Two Terra execution passes extracted only source JPEG objects in separate page ranges; a Sol review pass provided independent selection review; Codex visually inspected every final asset and consolidated the map. Existing p13/p14/p15/p30 Chapter 1 entries were retained. Weak-match, decorative, vector, duplicate, and already-diagrammed assets were intentionally excluded. This completes the Chapter 1 photo track and therefore the photo-extraction portion of the content-depth/photo audit build/fix pass.
- [x] **Restored visible in-topic navigation on small screens (Codex).** The topic list had been changed to a closed-by-default native `<details>` disclosure, which hid every sibling topic link after a learner opened a topic. It now renders open by default, so subtopic links remain visible and still may be collapsed manually. Added a `TopicList` regression assertion for the `open` state.
- [x] **Completed independent production UI/UX review and follow-up fixes (Codex).** Chrome DevTools reviewed the deployed home page; representative CH1, CH2, CH3, and CH4 topic shapes; the Tutor’s grounded response/citation flow; the CH99 empty state; and mobile topic navigation. No console errors or failed document/fetch requests were observed. Fixed the selected-subtopic state so it has a persistent tinted background, bold label, and teal marker; fixed compact-section heading order; and aligned citation-link accessible names with their visible source text. Production Lighthouse snapshot audits passed at 100 for accessibility, best practices, SEO, and agentic browsing on both desktop and mobile. Shipped in `ea4b81e` and `2881c68`.
