# Public Supabase client configuration (used by web/src/lib/supabase/server.ts)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Direct Postgres connection for scripts/import_reviewed_content.py --apply
COURSE_BRAIN_DATABASE_URL=

# Grounded tutor LLM fallback (web/src/lib/tutor/llm-fallback.ts) — used when
# the free keyword matcher finds no match; also set in the Vercel project's
# env vars for production/preview/development.
DEEPSEEK_API_KEY=
